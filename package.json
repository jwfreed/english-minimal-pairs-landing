{
  "name": "english-minimal-pairs-landing",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && touch dist/CNAME && echo 'soundwise.com' > dist/CNAME"
  },
  "devDependencies": {
    "vite": "^7.1.7"
  }
}
